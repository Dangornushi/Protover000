class Calc {
    calc( int a and int b ) => int {
        int result = a + b;
        return result;
    }
}
fn main => int {
    int x = 10;
    int y = 2;
    calcclass = Calc;
    put(calcclass.calc(x and y));
    return 0;
}
